#pragma once
#include <iostream>
#include <vector>
#include <ctime>
#include <cmath>

using namespace std;
using namespace sf;

void SelectSort(vector<int>& mass, int& M, int& C);
void SelectSortOpt(vector<int>& mass, int& M, int& C);