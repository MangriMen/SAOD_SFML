#include <iostream>
#include <vector>
#include "List.hpp"
#include "Queue.hpp"
#include "Stack.hpp"

using namespace std;

void split(List<int> S, List<int>* a, List<int>* b, int n) {
    a.push_back(S.head);
    b.push_back(S.head->pNext);
    n = 1;
    while (p != nullptr) {
        n++;
        k->next = p->next;
        k = p;
        p = p->Next;
    }
}

void MergeSort() {
    int q, r;
    while (q != 0 && r != 0) {
        if (a->Data <= b->Data) {
            //
            q--;
        }
        else {
            //
            r--;
        }
    }
    while (q > 0) {
        //
        q--;
    }
    while (r > 0) {
        //
        r--;
    }
}